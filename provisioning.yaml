version: 1

extensions:
  - module: provisioner_comfyui
    config:
      comfyui_dir: "/workspace/ComfyUI"
      # Тут ти можеш вказати список воркфлоу через дефіс
      workflows:
        - "https://raw.githubusercontent.com/nakkalashnikov/vast-ai-scripts/refs/heads/main/flux_photo_workflow.json"
        - "https://raw.githubusercontent.com/nakkalashnikov/vast-ai-scripts/refs/heads/main/wan2.2_video_workflow.json"
